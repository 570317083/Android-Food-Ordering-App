<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.naman.foodorderingapplication">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACTION_SEND" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".buddyscafe" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginPage" />
        <activity android:name=".ccd" />
        <activity android:name=".mitticafe" />
        <activity android:name=".yolocafe" />
        <activity android:name=".mochacafe" />
        <activity android:name=".cafeo2" />
        <activity android:name=".ChaiKaapi" />
        <activity android:name=".BeansCafe"></activity>
    </application>

</manifest>